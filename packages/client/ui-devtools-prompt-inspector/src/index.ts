/** Host loader entry for the prompt-inspector browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior; the inspector reads durable session events client-side. */
export function apply(): void {}
