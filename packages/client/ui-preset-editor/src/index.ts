/**
 * Host loader entry for @deepseek-ai/dsh-client-ui-preset-editor.
 * @module @deepseek-ai/dsh-client-ui-preset-editor
 */

/** Host plugin body — no host behavior. */
export function apply(): void {}
