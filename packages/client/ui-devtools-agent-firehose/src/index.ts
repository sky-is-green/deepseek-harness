/** Host loader entry for the agent-firehose browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior; the firehose reads durable session events client-side. */
export function apply(): void {}
