/** Host loader entry for the failure-forensics browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior; the fold lives in dsh-failure-forensics. */
export function apply(): void {}
