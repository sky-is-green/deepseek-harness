/**
 * Host loader entry for @deepseek-ai/dsh-client-ui-models-manager.
 * No host-side behavior — browser half owns the manager.
 * @module @deepseek-ai/dsh-client-ui-models-manager
 */

/** Host plugin body — no host behavior. */
export function apply(): void {}
