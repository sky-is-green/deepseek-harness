/**
 * Client-namespace pure-type outlet of the `liveTurnMetrics` projection key:
 * one import edge delivers the key merge and the payload type to browser
 * aggregates. Type-only by construction — the file is free of host-side
 * value imports, so no host Context merge enters a client program.
 *
 * @module @deepseek-ai/dsh-session-live-turn-metrics/client
 */

export type { LiveTurnMetricsPhase, LiveTurnMetricsView } from './types.ts'
export type {} from './types.ts'
